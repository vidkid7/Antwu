import type { Metadata } from 'next';
import { DocumentsPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'Document library', description: 'Access published ANTWU documents, regulations, directives and resources.', alternates: { canonical: '/documents' } };
export default DocumentsPageContent;

