import type { Metadata } from 'next';
import { MembershipPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'Membership registration', description: 'Submit your ANTWU membership application and supporting documents.', alternates: { canonical: '/membership' } };
export default MembershipPageContent;

