import type { Metadata } from 'next';
import { AboutPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'About our union', description: 'The official introduction, history and objectives of the All Nepal Transport Workers Union.', alternates: { canonical: '/about' } };
export default AboutPageContent;

