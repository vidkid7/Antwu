import type { Metadata } from 'next';
import { GalleryPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'Photo and video gallery', description: 'Explore photos and videos from the ANTWU archive.', alternates: { canonical: '/gallery' } };
export default GalleryPageContent;

