import type { Metadata } from 'next';
import { BlogPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'Union stories', description: 'Published stories and perspectives from ANTWU.', alternates: { canonical: '/blog' } };
export default BlogPageContent;

