import type { Metadata } from 'next';
import { UpdatesPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'Updates and notices', description: 'Official ANTWU notices, press releases, tenders and news.', alternates: { canonical: '/updates' } };
export default UpdatesPageContent;

