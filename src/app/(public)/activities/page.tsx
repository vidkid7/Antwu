import type { Metadata } from 'next';
import { ActivitiesPageContent } from '@/components/public-pages';
export const metadata: Metadata = { title: 'Union activities', description: 'Meetings, campaigns and activities from ANTWU.', alternates: { canonical: '/activities' } };
export default ActivitiesPageContent;

